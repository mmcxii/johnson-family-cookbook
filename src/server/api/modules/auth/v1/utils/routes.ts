export enum AuthV1Routes {
  Login = "/login",
  LoginSchema = "/login/schema",
  Logout = "/logout",
  RefreshTokens = "/refresh-tokens",
  RegisterUser = "/register-user",
  RegisterUserSchema = "/register-user/schema",
  User = "/user",
}
